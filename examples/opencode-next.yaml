nodes:
  - id: seed
    run: echo "5"
  - id: next
    depends_on: [seed]
    run: opencode run "Return only the integer that comes right after {{ seed.output }}. Output just the number, nothing else."
